# Figure 1 export

Open `/#/figure-1` in the app to view the 2¡Á2 Delay Pattern Overview figure.

Suggested export workflow:
1. Run `npm run dev`
2. Open the figure page
3. Use browser screenshot/export to save a high-resolution PNG

The figure is built with configurable ECharts options, so you can replace the illustrative values with computed statistics later.
