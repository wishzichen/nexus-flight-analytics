# 航班延误分析系统 - 使用说明

## 当前状态

✅ **前端已启动**: http://localhost:3000
⚠️ **数据状态**: 使用模拟数据（Mock Data）

---

## 安装 R 环境

### Windows 安装步骤

1. **下载 R**
   - 访问 https://cran.r-project.org/bin/windows/base/
   - 下载最新版 R (如 R-4.4.x)
   - 运行安装程序，使用默认设置

2. **验证安装**
   打开 PowerShell 或 CMD，运行：
   ```powershell
   Rscript --version
   ```

3. **安装 R 包**
   打开 R 或 RStudio，运行：
   ```r
   install.packages(c("nycflights13", "jsonlite", "dplyr", "lubridate", "tidyr"))
   ```

4. **生成数据**
   在项目目录下运行：
   ```powershell
   cd scripts
   Rscript run_all_analyses.R
   ```

---

## 启动系统

```powershell
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

访问 http://localhost:3000 查看系统。

---

## 系统功能

### 8 个分析模块

| 模块 | 功能 | 图表类型 |
|------|------|----------|
| 总览仪表板 | 整体延误概况 | KPI卡片、热力图、环形图 |
| 时间规律 | 24小时/月份/星期分析 | 折线图、面积图 |
| 航线分析 | 目的地延误排名 | 条形图、气泡图 |
| 空中追回 | 延误航班追回能力 | 散点图、箱线图 |
| 航司表现 | 航司准点率对比 | 气泡图、象限图 |
| 延误传导 | 同机延误传播 | Sankey图、折线图 |
| 延误归因 | 机龄vs天气分析 | 雷达图、特征重要性 |
| 数据探索 | 航班明细查询 | 数据表、筛选器 |

---

## 技术栈

- **前端**: React 19 + TypeScript + Vite + Tailwind CSS
- **图表**: ECharts
- **后端**: Express.js
- **数据分析**: R (nycflights13)